import { Card } from "@/components/ui/card";
import React from "react";

const KelolaCategory = ({ id }: { id: string }) => {
  return (
    <Card className="w-full h-full p-4 bg-white border border-gray-200 shadow-sm rounded-md">
      <div className="w-full flex justify-content-end">ToggleEventSta</div>
    </Card>
  );
};

export default KelolaCategory;
